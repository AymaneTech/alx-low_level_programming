#include <stdio.h>

/**
 * Main - Entry point
 * Description: A C program that prints "with proper grammar,, but the outcome
 * Is a piece of arts" followed by new line.
 *
 * @argc Argument count
 * @argv Argument vector
 *
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[]) {
  printf("with proper grammar, but the outcome is a piece of arts\n");
  return (0);
}
